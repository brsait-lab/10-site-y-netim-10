openapi: 3.1.0
info:
  title: Api
  version: 0.1.0
  description: API specification
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: auth
    description: Authentication
  - name: users
    description: User management
  - name: sites
    description: Site management
  - name: notifications
    description: Notifications
  - name: payments
    description: Payments
  - name: messages
    description: Messaging
  - name: chats
    description: Chat management
  - name: packages
    description: Package tracking
  - name: vendors
    description: Vendor management
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      description: Returns server health status
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /auth/login:
    post:
      operationId: login
      tags: [auth]
      summary: Login
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
      responses:
        "200":
          description: Login successful
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "401":
          description: Invalid credentials
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /auth/register:
    post:
      operationId: register
      tags: [auth]
      summary: Register
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterRequest"
      responses:
        "200":
          description: Registration result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RegisterResult"

  /auth/me:
    get:
      operationId: getMe
      tags: [auth]
      summary: Get current user
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Current user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserDto"

  /users:
    get:
      operationId: getUsers
      tags: [users]
      summary: Get users for the current user's site
      security:
        - bearerAuth: []
      parameters:
        - name: siteId
          in: query
          schema:
            type: string
      responses:
        "200":
          description: List of users
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/UserDto"

  /users/{id}:
    patch:
      operationId: updateUser
      tags: [users]
      summary: Update a user
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateUserRequest"
      responses:
        "200":
          description: Updated user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserDto"

  /users/{id}/approve:
    patch:
      operationId: approveUser
      tags: [users]
      summary: Approve a pending user
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Approved user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserDto"

  /users/{id}/reject:
    patch:
      operationId: rejectUser
      tags: [users]
      summary: Reject a pending user
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Rejected user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserDto"

  /sites:
    get:
      operationId: getSites
      tags: [sites]
      summary: Get all sites
      responses:
        "200":
          description: List of sites
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SiteDto"

  /notifications:
    get:
      operationId: getNotifications
      tags: [notifications]
      summary: Get notifications for the current user
      security:
        - bearerAuth: []
      responses:
        "200":
          description: List of notifications
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/NotificationDto"
    post:
      operationId: sendNotification
      tags: [notifications]
      summary: Send a notification
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendNotificationRequest"
      responses:
        "201":
          description: Created notification
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotificationDto"

  /notifications/{id}/read:
    patch:
      operationId: markNotificationRead
      tags: [notifications]
      summary: Mark notification as read
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Marked as read

  /payments:
    get:
      operationId: getPayments
      tags: [payments]
      summary: Get payments for the current site
      security:
        - bearerAuth: []
      responses:
        "200":
          description: List of payments
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PaymentDto"
    post:
      operationId: createPayment
      tags: [payments]
      summary: Create a payment
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePaymentRequest"
      responses:
        "201":
          description: Created payment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentDto"

  /user-payments:
    get:
      operationId: getUserPayments
      tags: [payments]
      summary: Get user payment records
      security:
        - bearerAuth: []
      responses:
        "200":
          description: List of user payments
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/UserPaymentDto"

  /user-payments/{id}/pay:
    patch:
      operationId: payDue
      tags: [payments]
      summary: Mark a user payment as paid
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Updated user payment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserPaymentDto"

  /chats:
    get:
      operationId: getChats
      tags: [chats]
      summary: Get chats for the current user
      security:
        - bearerAuth: []
      responses:
        "200":
          description: List of chats
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ChatDto"
    post:
      operationId: createChat
      tags: [chats]
      summary: Create a new chat
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateChatRequest"
      responses:
        "201":
          description: Created chat
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatDto"

  /chats/{id}/close:
    patch:
      operationId: closeChat
      tags: [chats]
      summary: Close a chat
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Closed chat
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatDto"

  /chats/{id}:
    delete:
      operationId: deleteChat
      tags: [chats]
      summary: Soft-delete a chat for the current user
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Deleted

  /chats/{id}/participants:
    get:
      operationId: getChatParticipants
      tags: [chats]
      summary: Get participants of a chat
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: List of participants
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ChatParticipantDto"

  /messages:
    get:
      operationId: getMessages
      tags: [messages]
      summary: Get messages for a chat
      security:
        - bearerAuth: []
      parameters:
        - name: chatId
          in: query
          schema:
            type: string
      responses:
        "200":
          description: List of messages
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/MessageDto"
    post:
      operationId: sendMessage
      tags: [messages]
      summary: Send a message
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendMessageRequest"
      responses:
        "201":
          description: Sent message
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageDto"

  /packages:
    get:
      operationId: getPackages
      tags: [packages]
      summary: Get packages for the current site
      security:
        - bearerAuth: []
      responses:
        "200":
          description: List of packages
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PackageDto"
    post:
      operationId: addPackage
      tags: [packages]
      summary: Add a package
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddPackageRequest"
      responses:
        "201":
          description: Added package
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PackageDto"

  /packages/{id}/status:
    patch:
      operationId: updatePackageStatus
      tags: [packages]
      summary: Update package status
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePackageStatusRequest"
      responses:
        "200":
          description: Updated package
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PackageDto"

  /vendor-categories:
    get:
      operationId: getVendorCategories
      tags: [vendors]
      summary: Get all vendor categories
      responses:
        "200":
          description: List of vendor categories
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/VendorCategoryDto"

  /vendors:
    get:
      operationId: getVendors
      tags: [vendors]
      summary: Get vendors
      security:
        - bearerAuth: []
      responses:
        "200":
          description: List of vendors
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/VendorDto"
    post:
      operationId: createVendor
      tags: [vendors]
      summary: Create a vendor profile
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateVendorRequest"
      responses:
        "201":
          description: Created vendor
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VendorDto"

  /vendors/{id}:
    patch:
      operationId: updateVendor
      tags: [vendors]
      summary: Update a vendor profile
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateVendorRequest"
      responses:
        "200":
          description: Updated vendor
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VendorDto"

  /vendor-requests:
    get:
      operationId: getVendorRequests
      tags: [vendors]
      summary: Get vendor requests
      security:
        - bearerAuth: []
      responses:
        "200":
          description: List of vendor requests
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/VendorRequestDto"
    post:
      operationId: createVendorRequest
      tags: [vendors]
      summary: Create a vendor request
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateVendorRequestInput"
      responses:
        "201":
          description: Created vendor request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VendorRequestDto"

  /vendor-requests/{id}/status:
    patch:
      operationId: updateVendorRequestStatus
      tags: [vendors]
      summary: Update vendor request status
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateVendorRequestStatusInput"
      responses:
        "200":
          description: Updated vendor request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VendorRequestDto"

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required: [status]

    ErrorResponse:
      type: object
      properties:
        message:
          type: string
      required: [message]

    LoginRequest:
      type: object
      properties:
        email:
          type: string
        password:
          type: string
        role:
          type: string
      required: [email, password, role]

    AuthResponse:
      type: object
      properties:
        user:
          $ref: "#/components/schemas/UserDto"
        token:
          type: string
      required: [user, token]

    RegisterRequest:
      type: object
      properties:
        name:
          type: string
        email:
          type: string
        password:
          type: string
        role:
          type: string
        phone:
          type: string
        siteId:
          type: string
        siteName:
          type: string
        siteAddress:
          type: string
        unitNo:
          type: string
        plates:
          type: array
          items:
            type: string
        businessName:
          type: string
        businessCategory:
          type: string
        businessDescription:
          type: string
        businessAddress:
          type: string
        latitude:
          type: number
          format: double
        longitude:
          type: number
          format: double
      required: [name, email, password, role, phone]

    RegisterResult:
      type: object
      properties:
        success:
          type: boolean
        message:
          type: string
        user:
          $ref: "#/components/schemas/UserDto"
        token:
          type: string
      required: [success, message]

    UserDto:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        email:
          type: string
        role:
          type: string
        siteId:
          type: string
        status:
          type: string
        phone:
          type: string
        unitNo:
          type: string
        plates:
          type: array
          items:
            type: string
        businessName:
          type: string
        businessCategory:
          type: string
        businessDescription:
          type: string
        businessAddress:
          type: string
        latitude:
          type: number
          format: double
        longitude:
          type: number
          format: double
        createdAt:
          type: string
      required: [id, name, email, role, siteId, status, phone, createdAt]

    UpdateUserRequest:
      type: object
      properties:
        name:
          type: string
        phone:
          type: string
        unitNo:
          type: string
        plates:
          type: array
          items:
            type: string
        businessName:
          type: string
        businessCategory:
          type: string
        businessDescription:
          type: string
        businessAddress:
          type: string
        latitude:
          type: number
          format: double
        longitude:
          type: number
          format: double

    SiteDto:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        address:
          type: string
        adminId:
          type: string
        createdAt:
          type: string
      required: [id, name, address, adminId, createdAt]

    NotificationDto:
      type: object
      properties:
        id:
          type: string
        type:
          type: string
        title:
          type: string
        message:
          type: string
        fromUserId:
          type: string
        fromName:
          type: string
        toRoles:
          type: array
          items:
            type: string
        toUserIds:
          type: array
          items:
            type: string
        siteId:
          type: string
        readBy:
          type: array
          items:
            type: string
        createdAt:
          type: string
      required: [id, type, title, message, fromUserId, fromName, siteId, readBy, createdAt]

    SendNotificationRequest:
      type: object
      properties:
        type:
          type: string
        title:
          type: string
        message:
          type: string
        fromUserId:
          type: string
        fromName:
          type: string
        toRoles:
          type: array
          items:
            type: string
        toUserIds:
          type: array
          items:
            type: string
        siteId:
          type: string
      required: [type, title, message, fromUserId, fromName, siteId]

    PaymentDto:
      type: object
      properties:
        id:
          type: string
        siteId:
          type: string
        title:
          type: string
        amount:
          type: number
          format: double
        dueDate:
          type: string
        type:
          type: string
        description:
          type: string
        createdAt:
          type: string
      required: [id, siteId, title, amount, dueDate, type, createdAt]

    CreatePaymentRequest:
      type: object
      properties:
        siteId:
          type: string
        title:
          type: string
        amount:
          type: number
          format: double
        dueDate:
          type: string
        type:
          type: string
        description:
          type: string
      required: [siteId, title, amount, dueDate, type]

    UserPaymentDto:
      type: object
      properties:
        id:
          type: string
        paymentId:
          type: string
        userId:
          type: string
        siteId:
          type: string
        status:
          type: string
        paidAt:
          type: string
      required: [id, paymentId, userId, siteId, status]

    ChatDto:
      type: object
      properties:
        id:
          type: string
        siteId:
          type: string
        title:
          type: string
        status:
          type: string
        createdBy:
          type: string
        closedBy:
          type: string
        closedAt:
          type: string
        createdAt:
          type: string
      required: [id, siteId, title, status, createdBy, createdAt]

    CreateChatRequest:
      type: object
      properties:
        title:
          type: string
        participantIds:
          type: array
          items:
            type: string
      required: [title]

    ChatParticipantDto:
      type: object
      properties:
        id:
          type: string
        chatId:
          type: string
        userId:
          type: string
        joinedAt:
          type: string
      required: [id, chatId, userId, joinedAt]

    MessageDto:
      type: object
      properties:
        id:
          type: string
        chatId:
          type: string
        fromId:
          type: string
        fromName:
          type: string
        content:
          type: string
        createdAt:
          type: string
      required: [id, chatId, fromId, fromName, content, createdAt]

    SendMessageRequest:
      type: object
      properties:
        chatId:
          type: string
        toId:
          type: string
        content:
          type: string
      required: [chatId, content]

    PackageDto:
      type: object
      properties:
        id:
          type: string
        siteId:
          type: string
        recipientUserId:
          type: string
        recipientName:
          type: string
        senderInfo:
          type: string
        description:
          type: string
        status:
          type: string
        receivedAt:
          type: string
        deliveredAt:
          type: string
      required: [id, siteId, recipientUserId, recipientName, senderInfo, description, status, receivedAt]

    AddPackageRequest:
      type: object
      properties:
        siteId:
          type: string
        recipientUserId:
          type: string
        recipientName:
          type: string
        senderInfo:
          type: string
        description:
          type: string
      required: [siteId, recipientUserId, recipientName, senderInfo, description]

    UpdatePackageStatusRequest:
      type: object
      properties:
        status:
          type: string
      required: [status]

    VendorCategoryDto:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        description:
          type: string
        createdAt:
          type: string
      required: [id, name, description, createdAt]

    VendorDto:
      type: object
      properties:
        id:
          type: string
        userId:
          type: string
        name:
          type: string
        category:
          type: string
        description:
          type: string
        phone:
          type: string
        address:
          type: string
        latitude:
          type: number
          format: double
        longitude:
          type: number
          format: double
        status:
          type: string
        createdAt:
          type: string
      required: [id, userId, name, category, description, phone, address, status, createdAt]

    CreateVendorRequest:
      type: object
      properties:
        name:
          type: string
        category:
          type: string
        description:
          type: string
        phone:
          type: string
        address:
          type: string
        latitude:
          type: number
          format: double
        longitude:
          type: number
          format: double
      required: [name, category]

    UpdateVendorRequest:
      type: object
      properties:
        name:
          type: string
        category:
          type: string
        description:
          type: string
        phone:
          type: string
        address:
          type: string
        latitude:
          type: number
          format: double
        longitude:
          type: number
          format: double
        status:
          type: string

    VendorRequestDto:
      type: object
      properties:
        id:
          type: string
        siteId:
          type: string
        requestedBy:
          type: string
        vendorId:
          type: string
        title:
          type: string
        description:
          type: string
        status:
          type: string
        assignedAt:
          type: string
        completedAt:
          type: string
        createdAt:
          type: string
      required: [id, siteId, requestedBy, title, description, status, createdAt]

    CreateVendorRequestInput:
      type: object
      properties:
        vendorId:
          type: string
        title:
          type: string
        description:
          type: string
      required: [title, description]

    UpdateVendorRequestStatusInput:
      type: object
      properties:
        status:
          type: string
      required: [status]
